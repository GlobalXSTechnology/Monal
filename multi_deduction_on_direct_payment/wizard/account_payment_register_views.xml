<odoo>
    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="inherit_id" ref="account.view_account_payment_register_form" />
        <field name="arch" type="xml">
    
            <xpath expr="//field[@name='writeoff_account_id']" position="after">
                <!-- attrs="{'invisible': [('payment_difference_handling', '!=', 'reconcile')]}" -->
                <div
                    invisible="payment_difference_handling !=  'reconcile'"
                    
                    groups="analytic.group_analytic_accounting"
                >
                    <label for="analytic_distribution" class="oe_edit_only" />
                    <field
                        name="analytic_distribution"
                        widget="analytic_distribution"
                        groups="analytic.group_analytic_accounting"
                        options="{'product_field': 'product_id', 'account_field': 'account_id', 'business_domain': 'expense'}"
                    />
                </div>
            </xpath>
            <xpath
                expr="/form/group/group[@name='group1']/div/div"
                position="attributes"
            >
                <!-- <attribute
                    name="attrs"
                >{'invisible': [('payment_difference_handling','!=','reconcile')]}</attribute> -->
                <attribute
                    name="invisible"
                >[('payment_difference_handling','!=','reconcile')]</attribute>
            </xpath>
            <!-- <xpath expr="/form/group/group[@name='group3']" position="attributes">
                <attribute name="invisible">[(not can_edit_wizard or can_group_payments and not group_payment)]</attribute>
            </xpath> -->
            <xpath expr="/form" position="inside">
                <!-- attrs="{'invisible': [('payment_difference_handling', '!=', 'reconcile_multi_deduct')]}" -->
                    <!-- invisible="payment_difference_handling !=  'reconcile_multi_deduct'" -->
                <group
                    colspan="4"
                    
                >
                    <field name="deduct_residual" invisible="1" />
                    <field name="deduct_analytic_distribution" invisible="1" />
                    <field
                        name="deduction_ids"
                        nolabel="1"
                        colspan="2"
                        
                    >
                    <!-- context="{'default_amount': deduct_residual, 'default_analytic_distribution': deduct_analytic_distribution}" -->
                        <list editable="bottom">
                            <field name="currency_id" column_invisible="1" />
                            <field name="is_open" column_invisible="1"/>
                            <!-- attrs="{'required': [('is_open', '=', False)]}" -->
                            <!-- required="is_open == False"  account_id not required for onchange-->
                            <field
                                name="account_id"
                                
                                
                            />
                            <field name="tax_id"/>
                            <field name="is_direct_deduct"/>
                            <field name="use_deducted_amount_for_tax"/>
                            <field
                                name="analytic_distribution"
                                widget="analytic_distribution"
                                groups="analytic.group_analytic_accounting"
                                options="{'product_field': 'product_id', 'account_field': 'account_id', 'business_domain': 'expense'}"
                            />
                            <field name="name" />
                            <field name="amount" sum="Total Deduction" />
                        </list>
                    </field>

                </group>
            </xpath>
            <!-- Huzaifa -->
            <xpath expr="//field[@name='communication']" position="after">
                <!-- <field name="cheq_no"></field> -->
            </xpath>

            <xpath expr="//field[@name='payment_date']" position="before">
                <field name="actual_amount"></field>
            </xpath>
        </field>
    </record>
</odoo>
